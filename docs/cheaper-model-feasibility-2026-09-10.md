# 2026-09-10：保持整轮 100 元的模型方案

用户明确选择保持原 `product-blueprint-2026-09-06` campaign 的 **CNY 100 累计上限**，评估更便宜模型。生产每天 CNY 20、首周观察和禁止自动提高预算的政策均不变。此记录是可行性分析，未改生产模型、候选部署、预算政策或已花费账本。

## 已确认的配置与价格

当前隔离候选为 Opus 4.8、主回答输出上限 4096。原费用预留器为所有调用使用同一个审定上限，每个已发出的调用永久占用该预留，失败或实际用量较少均不返还预算。按现有定价，仅输出部分的最低 962 次预留已经是 788.0704 元，输入尚未计入，无法在 100 元内执行。这是上限配置不兼容，不是已发生账单。

优先评估 `deepseek-flash`，官方当前版本为 DeepSeek-V4.1-Flash；旧 `deepseek-v4-flash-vision-exp` 已退役且请求转到新版，因此历史旧模型结果不能当作当前同模型基线。[型号及美元价格](https://api-docs.deepseek.com/quick_start/pricing/)

官方公开的高峰、未命中缓存价格为每百万输入/输出 token **2/8 元**；美元价格为 **0.3/1.2 美元**。必须先核实账户实际币种。美元方案暂沿用已批准的每美元 8 元预算估值上限，不将其称作现汇牌价；不使用缓存或闲时折扣缩小预留。[人民币价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)

该模型支持图片，官方文档列出每张最多 1024 个图片 token。现有 provider 通过两条消息传入文本和最多四张图片，未额外附加 OCR 正文；关闭 thinking，主回答仍可保留 4096、解释 768 的输出限制。[图片计量](https://api-docs.deepseek.com/guides/vision/)、[请求参数](https://api-docs.deepseek.com/api/create-chat-completion/)

## 整轮测算及边界

最低调用计划是新增题集 400 + 解释 80 + 两次解释拒绝检查预留 + Objective treatment 240 + 同一当前模型 legacy 240 = **962 次**。额外风险样本、连通性测试、恢复及失败尝试仍须加入最终总计划；不能逐批分别以“还剩 100 元”计费。

实测两套本地旧基线提示文本为 1746/1665 UTF-8 字节；当前服务端阅读文本 1594 字节。按最多 512 Unicode 标量、每个最多六个 JSON 编码字节构造解释长度包络后，文本最多 3550 字节。四图计量加逐文本字节一 token 的规划值为 7646；16,384 输入 token 情景另有 8738 余量。

**这尚不是审定的输入 token 上限。** 当前分词器/消息封装并未独立核定，官方图片计算器也提示估算可能略有差异。最终图片、风险场景、精确候选提示词及恢复/解释构造必须重新绑定；本地 CLI 取词结果不能代替正式执行器从精确版本重新导出。[计量说明](https://api-docs.deepseek.com/quick_start/token_usage/)

| 每次输入 / 输出情景 | 人民币计价，962 次预留 | 美元计价按 8 元估值，962 次预留 |
|---|---:|---:|
| 8192 / 4096 | 47.284224 元 | 56.741646 元 |
| 16384 / 4096 | 63.045632 元 | 75.655528 元 |
| 24576 / 4096 | 78.807040 元 | 94.568448 元 |

按中间情景，100 元尚有约 24～37 元容纳额外调用；按每次向上取整到人民币微元，分别可再预留 309/563 次。这个余量不授权随意重测，也不保证最终风险覆盖一定能放入。正式执行前仍须在同一账本检查全轮总量、价格时效和已占用额度。

## 当前不能开始付费测试的原因

2026-09-10 14:20 UTC，现有本地 DeepSeek 密钥访问 `/user/balance` 和 `/models` 均为 HTTP 401；Vercel 生产变量为 sensitive，单变量读取为 HTTP 403。未绕过平台保护、未请求模型、未将密钥写入报告。已请用户更新本机 `server/.env.local` 的 `DEEPSEEK_API_KEY`，无需在聊天中提供密钥。

有效密钥就绪后，先只读核实账户币种/余额及可用型号，再冻结新的隔离候选、费用上限和完整题集。正式材料布局、风险覆盖、评分及独立签署尚待完成；不能用此次价格分析跳过质量门槛。Cloudflare 官方 Cron 故障截至 14:22:50 UTC 仍为 identified，恢复时效验收也未通过。

私有证据位于 `.release-evidence/2026-09-10/cheaper-model/`：四份官方页面原件、提示长度探针、只读账户状态和 `feasibility.json`（SHA-256 `061311c07f861e6d42d44b7225db101fc7898d6cb80e027aae7aa0b06d254ff1`）。本次产品模型调用为 0；原预算账本尚未创建，政策 SHA-256 `2a0a2e15eb6d2a773c1e9e29aed7a4c4993d0127b1c9b03d6ff91724de22ed05` 未变。
