# 2026-09-11 发布进展

## 当前实现与 CI

评分修复提交 `f03a1b0f2f340833c1e8bc74d0f6e39ba9aca146` 的 [CI 34498548090](https://github.com/RotteSya/notch-SPI/actions/runs/34498548090) 已全部通过，10/10。Node 22.18/24.20 各 533 项；Postgres 16/17 × Node 22.18/24.20 各 654 项；Swift 302 项、2 项付费模型测试跳过、0 失败；Cloudflare 17 项。完整日志 SHA-256 为 `87c23ace01e4de29204e14b7252919816a67e563b8f32ffa488ed572fe7104c0`，精确提交与日志已归档。

本轮增加评测材料和记录，产品 App/服务端运行代码仍对应 `402d265` 的已验证输入，2.12 build 19 公证包没有重新打包或发布。CI 成功不代表模型质量、真实支付、生产迁移或灰度已经通过。

## 浏览器输入准备

从已策展 400 题中固定选取四种题型各一题，在本地可交互练习页中呈现；浏览器实际执行单选、短填、多选、排序的输入、保存和清空。页面只含题干与输入控件，答案、来源复核和预期状态另存；无模型调用或外部提交。独立材料设计复核明确有效 scope、风险预期和家族约束，报告 SHA-256 `68b05f4b3241c09e931f50811eda781f705edc6870550d38f53d61b35b0b17f1`。

共归档 12 张 JPEG：四个正常逻辑题的练习界面、同一阅读题的两张网页滚动截图，以及缺材料、裁切、模糊、多目标、范围外、标签歧义六个受控风险探针。网页与练习界面的同题变体不增加唯一题数；原 400 题/449 图索引不改。新截图索引 SHA-256 `b7eca8cda7653d17494b959ab8c413d08811444c33cea59d70deb5c58b74eec9`，是待准入呈现索引，不能直接派发为正式 manifest。

正常呈现只有四个独立逻辑题、五种呈现变体。网页两张是同页重叠滚动，不能当作真正跨页参考材料；本地受控页面也不能证明真实外部练习网站或用户输入分布的覆盖。每类异常只有一个探针，不足以支持 90%/95% 可靠性宣传。标签重复且存在两个偶数的歧义题无唯一金标，保持 `unlabelled + ambiguous`，不编造确定答案或 retake。

11 个呈现/风险条目的 12 张图片均通过真实服务图片解码和 SHA 校验、合法 scope 及完整 JSON 上传预检。包括重复最后题图和 512 字符最坏编码余量，最大请求 353,398 字节，小于 4,194,304 字节；预检无网络访问、无厂商请求。多目标探针使用 `target_count:1` 和完整图范围，让模型看到两个问题，不能用 HTTP 422 充当模型正确拒绝。当前 provider 不接收 rect 坐标，单题输入必须在实际像素中有明确目标。

初次截图揭示测试页面的排序来源只读到了渲染元数据，导致正文和表格缺失；已改为按 SHA 绑定回溯原始自编题，渲染前断言正文非空。失败页面/截图保留，修复后的表格和题干另存。浏览器 fullPage 在 DPR 2 下返回缩小内容，正常样本改用默认视口截图；裁切探针仍包含缩放伪影，单列为受控裁切与缩放混合情况，不声称自然截图。图片未经后期编辑。

最终独立 AI 审者逐张以原尺寸观察 12/12 张图，并核对源文、表格、候选答案、文件 SHA、家族和 scope。四个正常逻辑题的五种呈现答案一致，六个风险预期有依据，未发现当前截图阻断；裁切伪影、部分底部署名不在视口、JPEG media type 和代表性限制均保留。完整报告 SHA-256 `753e78a6bac2ce57db50a8f95c512172a4d625a3d456b74676949f590193c49c`，为已披露的非盲 AI 复核，正式批准仍为 false。临时浏览器页面及仅监听回环地址的材料服务器已关闭。

## 成本和发布条件

用户选择的整轮 100 CNY 上限保持，政策 SHA-256 `2a0a2e15eb6d2a773c1e9e29aed7a4c4993d0127b1c9b03d6ff91724de22ed05` 不变。两个工作目录均未创建本轮预算账本，产品模型调用仍为 0。更便宜模型目前只有 [费用可行性测算](cheaper-model-feasibility-2026-09-10.md)，有效本地密钥、账户币种、最终调用数量及可执行的保守输入上限仍待核定，不切换生产模型。

本次只读查询 [Cloudflare 官方事件](https://www.cloudflarestatus.com/incidents/sjs8s0q2x4hw) 仍为 `identified`、`resolved_at=null`。保留免费定时器方案，生产/候选调度未重新启用；官方恢复后须重新做实际触发和到期释放验收。生产新版本尚未激活，上线后每天 20 CNY 模型费用上限和首周观察尚不能按已生效处理。

后续仍需真实外部网页/练习场景和正式题集范围、独立授权签署、模型质量与至少 80 个解释复核、同候选模型 240 题双路径基线、实机最终结果 UI、恢复时效、支付对账、生产迁移以及规定灰度/观察窗口。截图和软件测试不替代这些发布条件。沿用既有授权推进，暂不重复请求用户已授予的测试、独立 AI 审题或部署权限。

## 真实外部练习页与出版错键

后续已获取 [Wikibooks 的实际代数练习页面](https://en.wikibooks.org/wiki/CLEP_College_Algebra/Algebraic_Operations#Exercises)，公开 API 固定到 revision `3821529`（2021-03-27 21:06:33 UTC，page ID 413505）。源 API 文件 SHA-256 `26ebc199ec561c276b03c8702c55f3ab673c823d6b71fef0ef19fbd7d43c4cc1`；正文原文、作者归属历史链接、当前页脚许可和实际截图分别归档。页面当前标明 CC BY-SA 4.0，按[官方许可](https://creativecommons.org/licenses/by-sa/4.0/)保留署名、许可和改动说明，派生材料分享时沿用相同许可；不宣称出版方认可产品。

固定选择 Exercises 区首个完整短填作为补充候选；它有唯一解 7，而网站答案键为 −7。由题干可得 `2x+4=3x−3`，正确值代回两侧均为18；出版值代回为−10与−24。原题文和错键原样留档，修订仅在独立金标记录中。旁边 Q2 的方程解为−43/70，五个选项均不含此值，网站却标第二项−19/22；该题不准入正常单选集，不能把出版方错键写入评测真值。

本次保留两张有效浏览器原始 JPEG：一道完整短填和默认 1280×720 视口中的多个独立问题。浏览器 clip 两次返回错误位置，失败图排除；正常题通过支持的视口/滚动操作隔离，实际视口1280×160，题面完整、输入为空且没有下一题。该窄视口属于受控选区方式，不宣称标准用户窗口。截图来自真实外部页面，未改写DOM/题文、未后期编辑或提交网站评分；真实来源与人为选区条件分别记录。

补充索引 SHA-256 `f206e97cd18c5582589f1bc93d6266388141617e985b529ef908f6d7509df373`，不覆盖原400库存，也尚未改变正式题数。两条输入通过实际图片解码、SHA、scope和请求大小预检，最大222,815字节；短填数值政策接受7、7.0、14/2，拒绝−7和无依据的kg单位。整页风险仍使用合法完整范围，预期由模型返回多个目标终止，不使用接口错误替代识别结果。同页正常题与风险必须保持同一划分；一题只能补充实际外部布局证据，不能证明广泛用户分布或未见过的模型训练内容。

前一文档提交 `aa2c25efeddad2cbdc40a45c1403f0ba0922e78d` 的 [CI 34549506973](https://github.com/RotteSya/notch-SPI/actions/runs/34549506973) 已10/10完成，完整日志摘要 `98aaf39776e7d65c350adb6e72c233cc6b60657756c8eeb1f500d328061b38b8`。本轮没有产品代码、模型预算或生产变更，产品模型调用仍为0。材料正式组合、签署及其余发布条件继续待完成。

最终独立 AI 审者实际查看两张有效图和两张排除图，复算并确认短填纠错、无正确选项的单选hold，以及多目标预期；扫描原400与策展400两个索引和18个绑定源文件，未发现新短填的相同题文或特征短语。精确报告 SHA-256 `ea2c64db369fa95aeaea6ded3766d55b21516a77e71516993e273b16be665ebd`。审者明确没有见证浏览器捕获或重跑根代理的请求预检，许可按根代理归档引用，不将这些工作冒充独立现场测试；正式manifest批准仍为false。


## 小用户量发布调整与截图修复集成（17时后）

用户明确要求按当前少量存量用户加快发布，已用[当前发布流程](small-user-release-2026-09-11.md)覆盖四档各72小时/200请求及至少12天等待。必要验收通过即可向存量开放，七天费用与28天产品观察放到实际上线后；不降低既有模型质量、财务和数据完整性要求，整轮100 CNY与生产每日20 CNY保持。

核查发现本地已有同日截图授权与备用截图修复，包含6个产品源码文件和5个测试文件，现按原字节合入发行分支，保留用户工作目录其他改动。旧402d265公证包不含这些修复，不能继续作为最终新版包；本轮重新构建验证。细节见[截图恢复记录](capture-timeout-recovery-2026-09-11.md)。

集成后的显式实机Swift测试320项、2项付费测试跳过、0失败；连续3次正常截图0.689/0.283/0.281秒，注入主路径超时后3次实际系统命令截图0.415/0.389/0.376秒，临时目录和图像清理通过。完整scripts/verify.sh通过：repo-health、TypeScript、533项Node测试、隔离服务器smoke、Swift320项（默认4跳过）及arm64 release构建。没有因复制旧测试日志宣布新代码通过。

新DEBUG二进制另签为独立QA身份，在本机使用实际截图、生产请求/结算路径和受控模型响应。3次截图到编码319/163/181ms；第二次新合约请求在上游结算后断流。真实界面恢复显示“答案已恢复·未追加扣题”和答案B，随后“解释已生成”、原答案B和解释正文可读；账户27题、累计3次主查题，数据库仅3次settled、无held，正常退出0。该结果基于真实辅助功能树，面板截屏像素全黑仍不冒充视觉排版验收；受控响应不算模型正确率。用户正在用的/Applications应用未改动，两个隔离QA/服务器分别关闭。

Cloudflare官方事件本轮仍identified，但按加快推进的判断，恢复一次隔离候选cron进行有界实测，不等待全平台公告清零；生产cron仍为空。候选会用真实到期持有与账本核对，无产品模型费用；实际结果另归档，配置成功不等于定时恢复已通过。
