# 低强度答题与普通解释候选

更新：a943700的mixed16调用已完成，答案8/8正确、解释8/8生成完成，但严格正确一致只有7/8。gsm8k-test-1145的解释先报正确答案291冲突，再自行算出291并称一致，属于严重矛盾。普通模式解释暂不作为已放行策略。完整证据和独立报告位于 `native/.release-evidence/2026-09-13/mixed-candidate/`，结果SHA为 `ef534529ef56742189fa359211beb8728fb3fa16a504649fe06bf63d1e13807a`。

本轮新增共48次真实模型调用，无重试；原始最高费用预留全部保留，累计保守消耗3.918339元，剩余23.081661元，上限27元。三个隔离候选均已正常关闭，无持有中请求。a943700的CI34705799767已10/10通过。

已准备服务器配置 `EXPLANATION_MAX_TOKENS`：默认768保持，显式值必须为1..4096整数，供应商还会取它和相应通道总输出上限的较小值。它只支持下一候选冻结与费用准入，不会自动提高生产或测试输出上限。未来配置2048等值时，必须更新当天解释费用上界；不能沿用这48次的768证明。新候选需针对正确/错误父答案的一致性开展冻结验证，避免继续在同8道已见题上无止境调整。

8ab68ed 的真实隔离对照已完成32次调用：固定8题分别运行none和low答题及解释。独立逐图、逐响应复核结果为none答案3/8正确，low为8/8；low解释7/8可用且这7条均正确一致，另1条在总输出达到768 token后失败。没有原始供应商finish_reason，不能断言768全部用于隐藏思考。none的一条排序解释静默改写错误父答案，另四条正文显式纠错但结构一致性标记推断仍误报true。

这是已见诊断题，不是独立留出集或正式质量通过。完整响应、父子关系和审题报告在 `native/.release-evidence/2026-09-13/thinking-candidate/`；旧报告和原始评测不覆盖。

下一候选保留low答题，对解释显式设none。新增两个配置：`OFFICIAL_DEEPSEEK_EXPLANATION_REASONING_EFFORT`、`OBJECTIVE_RESULT_V1_DEEPSEEK_EXPLANATION_REASONING_EFFORT`，均接受none/low/high/max。缺省沿用原答题思考行为；Objective没有显式答题设置时继承官方解释设置，否则解释默认跟随Objective自己的答题设置。显式Objective解释配置只改变该通道的解释，不改变control或恢复答案。

服务端辅助请求路由给内部Provider请求赋purpose，客户端不能借同名字段改变策略。仅explain使用解释配置；answer和recover仍使用答题策略。解释总输出上限仍为768，同父最多一次尝试、额外扣题0均保持。非法配置使对应DeepSeek通道拒绝服务。

mixed配置的本地44项定向、完整Node557项和类型检查通过，独立代码复核未发现新增阻断。实际16调用使用了新隔离数据库、新配置版本和新生成的low父答案。随后可配置解释上限的完整Node560项、48项定向和类型检查也通过；两者都是软件证据，不替代解释质量。

第一次32调用完成时，完整最高费用预留1.671168元保留，累计保守消耗3.082755元。随后mixed新增最高预留0.835584元，得到本页开头的最新合计。三测试设备均只扣8题、各余22题，无holding；实例已正常关闭。此前模型账户余额仍显示26.13元，不能因此把新调用当免费。尚未结算这48条用量，生产未切换。

独立代码审查还指出既有跨配置问题：辅助请求校验provider/model，但使用当前配置执行时仍记录父请求的configRevision。已补充跨revision的admission前拒绝，状态查询和重复solve回执同步关闭恢复能力；同revision解释/恢复仍可执行。修改模型策略时必须更新CLIENT_CONFIG_REVISION，同版本环境漂移不由本次修复检测。纯本地复现实验从新实例错误执行第二次调用变为503且仅保留原一次调用；不产生模型费用。
