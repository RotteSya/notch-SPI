# 27 元累计测试预算：有证据的费用结算

用户的测试预算为同一 campaign 累计 27 CNY，包含之前的全部调用；生产 20 CNY/日另计。不能换账本、换 campaign 或重置额度来绕过它。

截至结算执行前，原账本共有 969 条派发，逐次最高费用预留合计 26.915840 元。独立费用核验得到：966 条有完整正用量，按各次原有价格、币种和向上取整规则计算为 1.351171 元；另外 3 条用量未知，保留原最高费用 0.060416 元。因此可用于准入的保守消耗为 1.411587 元，仍可用 25.588413 元。这是内部保守费用记录，供应商账户余额按分变化约 1.07 元不作为逐请求精确账单。

原始 `historical-usage-cost-feasibility.json` 的 1.362435 元已撤回：两条 0/0 实际代表缺失用量，不能视为免费。原档案不覆盖。

## 工具行为

`scripts/reconcile-evaluation-budget.mjs` 默认只读预演，必须明确提供原账本、预算政策、结算批次及独立复核文件，并提供后二者的 SHA-256。只有 `--apply` 执行事务。该命令不访问网络、不请求模型。

批次逐条绑定原派发摘要、原价格对象和响应文件。只结算已完成、输入和输出均为正数、响应与账本一致的请求。未知、0/0、失败和超界记录保留原预留。结算以独立新增的不可改写记录表达，原派发和金额不修改；并发事务和重复执行不能重复释放。旧价格只在原派发时刻核验，不用今天更低的价格重算旧调用。

付费评测入口只打开已有匹配 campaign 的账本；拼错显式路径、空账本、错误 campaign 或新工作树缺省路径均拒绝，不能自动获得新的 27 元。

后续评测必须显式指定原账本，并重新核验当天的价格、候选部署和请求上界；9 月 11 日的旧价格证明和部署有效期已经过期。完成结算不代表模型质量通过，也不会自动触发新测试。

## 验证与执行状态

完整 Node 测试 551/551、本地预算及结算测试 21/21、TypeScript 检查通过。测试包括事务回滚、独立四进程并发、幂等、原记录不可变、未知用量、证据篡改和错误账本路径。独立审题代理另以临时账本完成 2 项反例测试，最终代码报告 revision2 通过，并独立复核真实 966 条批次。

2026-09-13 00:09 上海时间已完成真实结算：先备份原库，再只读预演，再事务新增 966 条结算；重新执行新增 0 条、释放 0 元，证明幂等。SQLite 完整性检查通过，原有 969 条派发、campaign 和 continuation 记录逐字段不变。结算后保守占用 1.411587 元、剩余 25.588413 元，上限 27 元。本次工作新增模型调用 0。证据位于 `native/.release-evidence/2026-09-13/evaluation-settlement/` 的 `applied.json`、`idempotent.json`、`after-verification.json` 和完整日志。

真实批次 `batch-966.json` SHA-256 为 `38b55e060e69ee1a4aabbdc5933ab6245334db703814fe9902516afc80083dcc`，位于 `native/.release-evidence/2026-09-12/evaluation-settlement/`。原始独立费用报告及逐条核验索引位于 `native/.release-evidence/2026-09-11/cheap-candidate/`。

当前新版本仍未公开。4ab2947 的扣题修复已通过 CI，签名安装包尚需恢复本机苹果公证凭证；Stripe 对账只读权限待原请求答复。本机 Vercel 凭证此前返回 403，本次连接器已成功解析生产域名，确认仍为 `dpl_BStwrGFdwhRC7FP3g2m6snSpcgfC`、`7ba96db` 旧版。新范围的解题与解释质量仍有真实失败，下一轮需新候选和独立结果记录。
