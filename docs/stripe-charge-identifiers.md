# Stripe 非卡付款编号兼容

2026-09-13 使用实际生产同一受限密钥只读核验历史 live Checkout，付款金额800 JPY、手续费32 JPY、到账768 JPY。Stripe 的 charges 接口返回 object=charge，但该订单 id 为 py_ 前缀；旧实现只允许 ch_，即使四个读取接口全部200也会在财务解析处报错。

付款订单、退款归属、争议归属、财务资源通知及余额交易来源现在同时接受 ch_ 和 py_。前缀后的长度/字符、确切资源归属、金额/币种与交易去重检查不变；未知前缀、路径片段、跨订单编号仍拒绝。既有历史测试订单保留，不用 live 密钥把测试记录伪装为现金收入。

53项支付相关检查、571项完整Node测试及类型检查通过。用修复后的精确支付源文件再次读取同一live订单，财务归一化和手续费汇总通过；无付款、退款、充值或生产数据库写入。临时受保护探针已删除。原未修复响应和修复响应分别在私有 finance-runtime-v3 与 finance-runtime-fixed 证据目录保存。

模型评测仍绑定94b2d17候选，其服务、提示词和参数保持原样；此修复仅进入最终支付构建，需记录相应构建与已评测模型组件的字节一致性。
